Task: {{task}}

Use `subagent` with `agentScope: "both"` to delegate to `frontend-ui-ux-engineer` or `document-writer` as appropriate.

Focus on clarity, polish, naming, UX, documentation quality, and user-facing coherence.
Do not make code changes unless explicitly requested.
