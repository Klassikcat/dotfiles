#!/bin/bash

OUTPUT="${1:-HDMI-1}"
FILENAME="$HOME/Videos/Screencasts/Screencast-$(date +%Y%m%d-%H%M%S).mp4"

# 디렉토리가 없으면 생성
mkdir -p "$HOME/Videos/Screencasts"

wf-recorder -o "$OUTPUT" -f "$FILENAME"
