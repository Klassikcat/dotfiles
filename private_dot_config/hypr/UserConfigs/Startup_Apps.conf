# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Commands and Apps to be executed at launch

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

$wallDIR=$HOME/Pictures/wallpapers
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh
$livewallpaper="$HOME/Videos/LiveWallpaper/yuuka-eating-blue-archive-moewalls-com.mp4"

# wallpaper stuff 
exec-once = swww-daemon --format xrgb
exec-once = $scriptsDir/SwwwMonitorRestore.sh

# wallpaper random
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes

# Startup
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE XDG_SESSION_TYPE GTK_IM_MODULE QT_IM_MODULE XMODIFIERS INPUT_METHOD SDL_IM_MODULE GLFW_IM_MODULE MOZ_ENABLE_WAYLAND
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP HYPRLAND_INSTANCE_SIGNATURE XDG_SESSION_TYPE GTK_IM_MODULE QT_IM_MODULE XMODIFIERS INPUT_METHOD SDL_IM_MODULE GLFW_IM_MODULE MOZ_ENABLE_WAYLAND
exec-once = systemctl --user start hyprland-session.target
exec-once = $scriptsDir/AutoRotate.sh
# Fix env-timing issues: after env import, start heliocron-based theme services under Hyprland session
# NOTE: systemd user units for hyprland-theme@sunrise-light/sunset-dark are disabled; Hyprland owns their lifecycle.
exec-once = systemctl --user start hyprland-theme@sunrise-light.service hyprland-theme@sunset-dark.service

# Initialize Drop Down terminal - See Bug#810 https://github.com/JaKooLit/Hyprland-Dots/issues/810#issuecomment-3351947644
exec-once = $HOME/.config/hypr/scripts/Dropterminal.sh ghostty &


# Polkit (Polkit Gnome / KDE)
exec-once = $scriptsDir/Polkit.sh

# starup apps
exec-once = nm-applet --indicator
#exec-once = swaync
exec-once = ags
exec-once = blueman-applet
#exec-once = rog-control-center
# Waybar is managed by systemd user service: ~/.config/systemd/user/waybar.service
# exec-once = $scriptsDir/WaybarStart.sh
exec-once = qs # quickshell AGS Desktop Overview alternative

#clipboard manager
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Input Method (fcitx5 for Korean/Hangul)
exec-once = fcitx5 -d

# Rainbow borders
#exec-once = $UserScripts/RainbowBorders.sh

# hypridle is managed by the user systemd service to avoid duplicate ScreenSaver owners
exec-once = systemctl --user start hypridle.service

# toggle-theme starts or updates hyprsunset according to the selected theme.
# Do not start Hyprsunset.sh here; it always starts warm and can override Light/Dark.

# Hyprland theme service
# Restore Theme on Startup (calculates based on sun position)
exec-once = $HOME/.local/bin/hyprland-theme-init.sh

# Display brightness based on time (midnight: 30%, sunrise: 100%)
exec-once = $HOME/.local/bin/brightness-init.sh

# Do not force identity after theme init; it overrides Midnight's temperature.

# Here are list of features available but disabled by default
# exec-once = swww-daemon --format xrgb && swww img $HOME/Pictures/wallpapers/mecha-nostalgia.png  # persistent wallpaper

#gnome polkit for nixos
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh
