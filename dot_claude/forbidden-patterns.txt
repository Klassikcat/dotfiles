# ~/.claude/forbidden-patterns.txt
# 접근을 금지할 파일/디렉토리 패턴 목록
#
# 규칙:
#   - 한 줄에 하나씩
#   - # 으로 시작하는 줄은 주석
#   - ~ 는 홈 디렉토리로 자동 확장
#   - * 는 glob 와일드카드
#   - 경로 prefix 매칭 (디렉토리 끝에 / 붙이면 하위 전체 포함)
#
# 일시 허용: echo '경로' >> ~/.claude/.forbidden-allowlist

# ── SSH 키 ────────────────────────────────────────────────────────────────
~/.ssh/

# ── GPG / PGP 키 ──────────────────────────────────────────────────────────
~/.gnupg/

# ── 인증서 & 개인 키 파일 ────────────────────────────────────────────────
*.pem
*.key
*.pfx
*.p12
*id_rsa*
*id_ed25519*
*id_dsa*
*id_ecdsa*

# ── 환경 변수 / 시크릿 ────────────────────────────────────────────────────
*.env
.env.*
*.secret
*.secrets
*secrets.yaml
*secrets.yml
*secrets.json

# ── 클라우드 자격증명 ─────────────────────────────────────────────────────
~/.aws/credentials
~/.aws/config
~/.config/gcloud/
~/.kube/config

# ── Claude 자체 자격증명 ──────────────────────────────────────────────────
~/.claude/.credentials.json
~/.claude/settings.local.json
